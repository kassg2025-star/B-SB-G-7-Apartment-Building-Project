[build]
  command       = "npm ci --include=dev && chmod +x node_modules/.bin/vite && npm run build"
  publish       = "dist"

[build.environment]
  NODE_VERSION  = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
